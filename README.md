# 🚀 FAST DNS HALOTEL TANZANIA 🇹🇿

## PYTHON QUICK INSTALL 🔥

Copy and paste this single command to install:

```bash
apt update && apt install -y python3-pip wget && pip3 install psutil && cd /root && wget https://raw.githubusercontent.com/chiddy80/Halotel-Slow-DNS/main/DNSTT%20MODED/moded-slowdns.py -O moded-slowdns.py && python3 moded-slowdns.py --install
