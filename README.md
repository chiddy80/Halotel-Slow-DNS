# 🚀 FAST DNS HALOTEL TANZANIA 🇹🇿

## Python Scripted By Esimfreegb 🔥

Copy and paste this single command to install:

```bash
apt update
apt install -y curl && cd /root &&
curl -fsSL https://raw.githubusercontent.com/chiddy80/Halotel-Slow-DNS/main/DNSTT%20MODED/slow.sh -o slow.sh
